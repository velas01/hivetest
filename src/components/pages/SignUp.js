/*import React from 'react';
import '../../App.css';

export default function SignUp() {
  return <h1 className='sign-up'>LIKE & SUBSCRIBE</h1>;
} */

/*
import React, { useEffect } from 'react';
import '../../App.css';

export default function SignUp() {
    useEffect(() => {
        // Function to create the login form
        function createLoginForm() {
            // Create a form element
            const form = document.createElement('form');

            // Create a label for the email input
            const emailLabel = document.createElement('label');
            emailLabel.textContent = 'Email:';
            emailLabel.setAttribute('for', 'email');

            // Create the email input field
            const emailInput = document.createElement('input');
            emailInput.setAttribute('type', 'email');
            emailInput.setAttribute('id', 'email');
            emailInput.setAttribute('name', 'email');
            emailInput.setAttribute('required', true);

            // Create a label for the password input
            const passwordLabel = document.createElement('label');
            passwordLabel.textContent = 'Password:';
            passwordLabel.setAttribute('for', 'password');

            // Create the password input field
            const passwordInput = document.createElement('input');
            passwordInput.setAttribute('type', 'password');
            passwordInput.setAttribute('id', 'password');
            passwordInput.setAttribute('name', 'password');
            passwordInput.setAttribute('required', true);

            // Create the login button
            const loginButton = document.createElement('button');
            loginButton.textContent = 'Login';
            loginButton.setAttribute('type', 'submit');

            // Create the signup link
            const signupLink = document.createElement('p');
            signupLink.innerHTML = 'Not an Existing User? <a href="signup.html">Sign Up</a>';

            // Add the form elements to the form
            form.appendChild(emailLabel);
            form.appendChild(emailInput);
            form.appendChild(passwordLabel);
            form.appendChild(passwordInput);
            form.appendChild(loginButton);
            form.appendChild(signupLink);

            // Get the target element where you want to add the form
            const targetElement = document.getElementById('form-container');

            // Append the form to the target element
            targetElement.appendChild(form);
          }
        } );


    

    return (
        <div>
            {/* You can add any additional React components or content here */ //}
       /* </div>
    );
} */

